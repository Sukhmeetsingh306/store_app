import 'package:flutter/material.dart';

class UploadUserSellerScreen extends StatelessWidget {
  const UploadUserSellerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'Upload Screen',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}
