import 'package:flutter/material.dart';

class OrderUserSellerScreen extends StatelessWidget {
  const OrderUserSellerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'Order Screen',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}
