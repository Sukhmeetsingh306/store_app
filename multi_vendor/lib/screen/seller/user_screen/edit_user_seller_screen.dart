import 'package:flutter/material.dart';

class EditUserSellerScreen extends StatelessWidget {
  const EditUserSellerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'Edit Screen',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}
