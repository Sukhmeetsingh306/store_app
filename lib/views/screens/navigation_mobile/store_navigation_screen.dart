import 'package:flutter/material.dart';

class StoreNavigationScreen extends StatelessWidget {
  const StoreNavigationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text("store")),
    );
  }
}
