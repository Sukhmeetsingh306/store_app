import 'package:flutter/material.dart';

class CartNavigationScreen extends StatelessWidget {
  const CartNavigationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}