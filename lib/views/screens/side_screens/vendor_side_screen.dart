import 'package:flutter/cupertino.dart';

class VendorSideScreen extends StatelessWidget {
  static const String routeName = '/vendorScreen';
  const VendorSideScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'VendorSideScreen',
      ),
    );
  }
}