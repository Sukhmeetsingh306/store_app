import 'package:flutter/cupertino.dart';

class OrderSideScreen extends StatelessWidget {
  static const String routeName = '/orderScreen';
  const OrderSideScreen({super.key});
  

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'orderScreen',
      ),
    );
  }
}