import 'package:flutter/cupertino.dart';

class BuyerSideScreen extends StatelessWidget {
  static const String routeName = '/buyerScreen';
  const BuyerSideScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Buyer',
      ),
    );
  }
}
