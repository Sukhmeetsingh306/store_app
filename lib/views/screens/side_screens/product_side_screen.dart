import 'package:flutter/cupertino.dart';

class ProductSideScreen extends StatelessWidget {
  static const String routeName = '/productScreen';
  const ProductSideScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'ProductSideScreen',
      ),
    );
  }
}